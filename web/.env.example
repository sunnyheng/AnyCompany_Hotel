# Copy to .env.local and fill in the CDK stack outputs to run against a
# deployed stack ("live mode"). Leave unset to run in mock mode (no AWS).
VITE_AWS_REGION=us-east-1
VITE_USER_POOL_ID=
VITE_USER_POOL_CLIENT_ID=
VITE_API_URL=
VITE_STEP_UP_THRESHOLD=500
