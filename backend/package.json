{
  "name": "anycompany-hotel-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Lambda handlers for the AnyCompany Hotels step-up auth reference implementation",
  "type": "module",
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@aws-sdk/client-ses": "^3.600.0",
    "@aws-sdk/lib-dynamodb": "^3.600.0",
    "aws-jwt-verify": "^4.0.1"
  },
  "devDependencies": {
    "esbuild": "^0.24.2"
  }
}
